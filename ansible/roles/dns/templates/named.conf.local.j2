zone "{{ domain_name }}" {
    type master;
    file "/etc/bind/zones/db.{{ domain_name }}";
};

zone "{{ flipped_ip }}.in-addr.arpa" {
    type master;
    file "/etc/bind/zones/db.{{ flipped_ip }}.in-addr.arpa";
};
